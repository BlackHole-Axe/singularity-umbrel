#!/usr/bin/env bash
set -euo pipefail
HOOK_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck disable=SC1091
source "${HOOK_DIR}/common.sh"
singularity_clean_containers
echo "SINGULARITY (${SINGULARITY_APP_ID}): stopped before uninstall"
